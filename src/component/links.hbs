<div class="nav">
    {{#each links}}
    <div class="nav-item">
        <a class="nav-link {{#if @last includeZero=true}} disabled {{/if}}" href="{{href}}">{{text}}</a>
    </div>
    {{/each}}
</div>